# VinayakEnterprises — PROGRESS.md
> Last updated: 2026-04-30 14:48 IST  
> Phase: **Skeleton scaffold complete — no business logic yet**

---

## 1. Files Created

### Root
| File | Purpose |
|---|---|
| `VinayakEnterprises.slnx` | .NET 10 XML solution file (9 projects) |
| `Directory.Build.props` | Shared MSBuild settings: nullable, implicit usings, lang version, author info |
| `.gitignore` | Excludes bin/, obj/, tessdata binaries, keygen output |
| `PROMPT.md` | Master specification (unchanged, pre-existing) |

### src/VinayakEnterprises.App (WPF Host — `net10.0-windows`)
| File | Purpose |
|---|---|
| `VinayakEnterprises.App.csproj` | WPF project; refs all 7 sibling projects; publish config (single-exe, self-contained, win-x64) |
| `App.xaml` / `App.xaml.cs` | WPF entry point (dotnet template default — not yet wired to DI host) |
| `MainWindow.xaml` / `MainWindow.xaml.cs` | Shell window (dotnet template default — not yet styled) |
| `AssemblyInfo.cs` | WPF assembly attributes (auto-generated by template) |
| `appsettings.json` | 5 configurable values (SessionTimeout, WeightPolling, StableTolerance, CameraIndex, TessDataPath) |

### src/VinayakEnterprises.Core (Business Logic — `net10.0`)
| File | Purpose |
|---|---|
| `VinayakEnterprises.Core.csproj` | Classlib; CommunityToolkit.Mvvm, BCrypt.Net-Next, DI, Logging |

### src/VinayakEnterprises.Data (SQLite — `net10.0`)
| File | Purpose |
|---|---|
| `VinayakEnterprises.Data.csproj` | Classlib; Microsoft.Data.Sqlite, Dapper, DI, Logging |

### src/VinayakEnterprises.Hardware (Weighbridge Comms — `net10.0`)
| File | Purpose |
|---|---|
| `VinayakEnterprises.Hardware.csproj` | Classlib; System.IO.Ports, DI, Logging |

### src/VinayakEnterprises.Imaging (Camera & OCR — `net10.0`)
| File | Purpose |
|---|---|
| `VinayakEnterprises.Imaging.csproj` | Classlib; OpenCvSharp4 + runtime.win, Tesseract, DI, Logging |

### src/VinayakEnterprises.Reports (PDF & Excel — `net10.0`)
| File | Purpose |
|---|---|
| `VinayakEnterprises.Reports.csproj` | Classlib; QuestPDF, QRCoder, ClosedXML, DI, Logging |

### src/VinayakEnterprises.Notifications (WhatsApp & Audit — `net10.0`)
| File | Purpose |
|---|---|
| `VinayakEnterprises.Notifications.csproj` | Classlib; Selenium.WebDriver, WebDriverManager, Serilog (+File+Extensions.Logging), Newtonsoft.Json, DI, Logging |

### src/VinayakEnterprises.Licensing (License Engine — `net10.0`)
| File | Purpose |
|---|---|
| `VinayakEnterprises.Licensing.csproj` | Classlib; System.Management, DI, Logging |

### tools/VinayakEnterprises.KeygenTool (Vendor-only — `net10.0-windows`)
| File | Purpose |
|---|---|
| `VinayakEnterprises.KeygenTool.csproj` | WinForms project; System.Management |
| `Program.cs` | WinForms entry point (template default) |
| `Form1.cs` / `Form1.Designer.cs` | Default form (template default — to be replaced with keygen UI) |

---

## 2. Directories Created (Empty, with `.gitkeep`)

### App
```
Views/Auth, Views/Dashboard, Views/SlipEntry, Views/Masters,
Views/Reports, Views/Utility, Views/Settings, Views/Shared,
Resources/Themes, Resources/Languages, Resources/Fonts, Resources/Icons,
Converters, Behaviors
```

### Core
```
Models, ViewModels/Auth, ViewModels/Masters, ViewModels/Reports,
ViewModels/Settings, ViewModels/Utility,
Services/Interfaces, Services/Implementations, Helpers
```

### Data
```
Interfaces, Migrations, Repositories
```

### Hardware
```
Interfaces, Models, Protocols
```

### Imaging
```
Interfaces, Models, tessdata
```

### Reports
```
Interfaces, Models, Pdf, Excel
```

### Notifications
```
Interfaces, WhatsApp/Models, Audit/Models
```

### Licensing
```
Interfaces, Models
```

---

## 3. Features Completed

| # | Feature | Status |
|---|---|---|
| 1 | Solution file created (`.slnx`, 9 projects) | ✅ Done |
| 2 | All 9 projects created with correct TFMs (`net10.0` / `net10.0-windows`) | ✅ Done |
| 3 | Project reference DAG wired (App→all, Core→6, Notifications→Data) | ✅ Done |
| 4 | All NuGet packages installed across all 9 projects | ✅ Done |
| 5 | Full folder skeleton with `.gitkeep` files | ✅ Done |
| 6 | `appsettings.json` with 5 configurable keys | ✅ Done |
| 7 | `Directory.Build.props` shared settings | ✅ Done |
| 8 | `.gitignore` for build artifacts, tessdata, keygen | ✅ Done |
| 9 | App csproj: PublishSingleFile, SelfContained, win-x64, tessdata copy | ✅ Done |
| 10 | `dotnet build` — 0 errors, 0 warnings | ✅ Verified |

---

## 4. What Was In Progress When We Stopped

**Nothing was in progress.** The skeleton scaffold phase completed fully.  
The solution compiles successfully. No business logic code exists yet — only `.csproj` files, folder structure, and the template-generated App/MainWindow/KeygenTool boilerplate.

**The next phase is writing business logic code** starting with the Data layer.

---

## 5. Decisions Made (Not in PROMPT.md)

| Decision | What PROMPT.md Says | What Was Actually Done | Why |
|---|---|---|---|
| **Camera library** | "AForge.NET or OpenCvSharp4" | **OpenCvSharp4 only** — AForge removed entirely | User decision: AForge hasn't been updated since 2013 |
| **Micro-ORM** | "Microsoft.Data.Sqlite" only | **Dapper added** alongside Microsoft.Data.Sqlite | User approved for cleaner repository code |
| **ChromeDriver** | "Selenium WebDriver + ChromeDriver" | **WebDriverManager** NuGet instead of Selenium.WebDriver.ChromeDriver | User decision: auto-downloads correct driver, no dependency on system Chrome |
| **Target framework** | ".NET 8" | **.NET 10** (`net10.0` / `net10.0-windows`) | User correction: use latest .NET |
| **Config file** | Not mentioned | **appsettings.json** added to App with 5 keys | User requirement: SessionTimeout, WeightPolling, StableTolerance, CameraIndex, TessDataPath |
| **Solution format** | `.sln` implied | `.slnx` (new XML format) | .NET 10 SDK default — functionally equivalent |
| **Configuration packages** | Not mentioned | `Microsoft.Extensions.Configuration.Json` + `.Binder` v10.0.7 | Needed to read appsettings.json at runtime |

---

## 6. NuGet Package Summary

| Package | Version | Project(s) |
|---|---|---|
| CommunityToolkit.Mvvm | 8.4.2 | App, Core |
| MaterialDesignThemes | 5.3.1 | App |
| MaterialDesignColors | 5.3.1 | App |
| Microsoft.Extensions.Hosting | 10.0.7 | App |
| Microsoft.Extensions.DependencyInjection | 10.0.7 | App, Core, Data, Hardware, Imaging, Reports, Notifications, Licensing |
| Microsoft.Extensions.Logging.Abstractions | 10.0.7 | App, Core, Data, Hardware, Imaging, Reports, Notifications, Licensing |
| Microsoft.Extensions.Configuration.Json | 10.0.7 | App |
| Microsoft.Extensions.Configuration.Binder | 10.0.7 | App |
| BCrypt.Net-Next | 4.1.0 | Core |
| Microsoft.Data.Sqlite | 10.0.7 | Data |
| Dapper | latest | Data |
| System.IO.Ports | 10.0.7 | Hardware |
| OpenCvSharp4 | 4.13.0 | Imaging |
| OpenCvSharp4.runtime.win | 4.13.0 | Imaging |
| Tesseract | latest | Imaging |
| QuestPDF | 2026.2.4 | Reports |
| QRCoder | latest | Reports |
| ClosedXML | latest | Reports |
| Selenium.WebDriver | 4.43.0 | Notifications |
| WebDriverManager | 2.17.7 | Notifications |
| Serilog | latest | Notifications |
| Serilog.Sinks.File | latest | Notifications |
| Serilog.Extensions.Logging | latest | Notifications |
| Newtonsoft.Json | latest | Notifications |
| System.Management | 10.0.7 | Licensing, KeygenTool |

---

## 7. Next Step to Continue From

### Phase: **Business Logic Implementation**

Recommended implementation order:

1. **Data** — Define all domain models (`User`, `Customer`, `Supplier`, `Item`, `Vehicle`, `SlipEntry`, etc.), create `AppDbContext` connection factory, `SchemaInitializer` with CREATE TABLE + indexes, and all repository classes using Dapper
2. **Licensing** — `MachineIdGenerator` (WMI → SHA-256), `LicenseKeyValidator` (HMAC-SHA256), `LicenseStore` (DPAPI + registry), `ExpiryChecker` (startup + 24hr background), `ClockRollbackDetector`
3. **Core** — All service interfaces (`IAuthService`, `ISlipService`, `ILicenseService`, etc.), ViewModels using CommunityToolkit.Mvvm, `PasswordHasher` (BCrypt wrapper), `SessionTimer`
4. **Hardware** — `SerialWeightReader`, `UsbWeightReader`, `TcpWeightReader`, weight polling loop, stability detection
5. **Imaging** — `CameraService` (OpenCvSharp4), `FrameCapture`, `AnprService` (Tesseract), download eng+hin traineddata files
6. **Reports** — `WeighmentSlipDocument` (QuestPDF with QR code), `ExcelExporter` (ClosedXML)
7. **Notifications** — `WhatsAppService` (Selenium + WebDriverManager), `AuditLogger` (Serilog + SQLite)
8. **App** — DI host wiring in App.xaml.cs, MaterialDesignThemes setup, dark/light themes, EN/HI ResourceDictionaries, all XAML views, navigation, 7-segment font
9. **KeygenTool** — Keygen form UI, `LicenseKeyGenerator` using shared HMAC secret

### Suggested first command:
```
"Start implementing the Data layer — all domain models, AppDbContext, SchemaInitializer, and repository classes"
```

---
---

# Phase 2: Data Layer Implementation Report
> Last updated: 2026-04-30 15:15 IST  
> Phase: **Data layer scaffold complete — SQLite, Dapper Repositories, Domain Models**

## 1. Files Created

### src/VinayakEnterprises.Core (Business Logic)
| File | Purpose |
|---|---|
| `Models/BaseEntity.cs` | Common properties (`Id`, `IsDeleted`) for domain models |
| `Models/User.cs`, `Role.cs`, `Customer.cs`, `Supplier.cs`, `Item.cs`, `Vehicle.cs`, `Field01.cs`, `Field02.cs`, `Field03.cs`, `WBLocation.cs`, `CompanyMaster.cs`, `SystemSettings.cs`, `LicenseInfo.cs`, `HelpCode.cs`, `GoodsDispatch.cs`, `AuditLog.cs`, `SlipEntry.cs` | Domain models implemented as classes with properties, representing the database schema |

### src/VinayakEnterprises.Data (SQLite)
| File | Purpose |
|---|---|
| `AppDbContext.cs` | SQLite connection factory using `%APPDATA%\VinayakEnterprises\vinayak.db` |
| `Migrations/SchemaInitializer.cs` | Database initialization script (`CREATE TABLE IF NOT EXISTS` for all 17 tables) |
| `Interfaces/*.cs` | Generic `IRepository<T>` and 17 entity-specific interfaces (e.g., `IUserRepository`) |
| `Repositories/*.cs` | 17 entity-specific repository implementations using **Dapper** |

## 2. Features Completed
| # | Feature | Status |
|---|---|---|
| 1 | Domain Models (17 total classes in Core) | ✅ Done |
| 2 | `AppDbContext` and SQLite schema migration (`SchemaInitializer`) | ✅ Done |
| 3 | Generic `IRepository<T>` and 17 Entity-Specific Interfaces | ✅ Done |
| 4 | 17 Dapper Repository Implementations (handling Soft Deletes) | ✅ Done |
| 5 | Clean Architecture Dependencies Enforced (Circular dependency fixed) | ✅ Done |
| 6 | `dotnet build` — 0 errors, 0 warnings across all 9 projects | ✅ Verified |

## 3. What Was In Progress When We Stopped
**Nothing was in progress.** The Data layer phase completed fully.
The solution compiles successfully with 0 errors. The data layer is complete and ready to be used by services in the Core layer.

## 4. Decisions Made (Not in PROMPT.md)
| Decision | What PROMPT.md Says | What Was Actually Done | Why |
|---|---|---|---|
| **Clean Architecture References** | Core references all 6 | **Core dependencies removed**. Data references Core. | A circular dependency broke the build. In standard MVVM/Clean Architecture, Core is independent and infrastructure depends on Core to access domain models and interfaces. |
| **AppDbContext Usage** | Not detailed | **Factory method (`CreateConnection()`)** implemented | Dapper standard pattern. Repositories instantiate their own connections, avoiding scoped lifecycle issues with SQLite. |
| **Soft Delete Exemptions** | Soft deletes on "all tables" | **IsDeleted omitted** from `SystemSettings`, `CompanyMaster`, and `LicenseInfo` | User specified that system-level configuration tables should never be soft-deleted. |

## 5. Next Step to Continue From
### Phase: **Licensing or Core Services Implementation**

Recommended implementation order (Updated):
1. **Licensing** — `MachineIdGenerator` (WMI → SHA-256), `LicenseKeyValidator` (HMAC-SHA256), `LicenseStore` (DPAPI + registry), `ExpiryChecker` (startup + 24hr background), `ClockRollbackDetector`
2. **Core Services** — All service interfaces (`IAuthService`, `ISlipService`, `ILicenseService`, etc.), `PasswordHasher` (BCrypt wrapper), `SessionTimer`
3. **Hardware** — `SerialWeightReader`, `UsbWeightReader`, `TcpWeightReader`, weight polling loop, stability detection
4. **Imaging** — `CameraService` (OpenCvSharp4), `FrameCapture`, `AnprService` (Tesseract), download eng+hin traineddata files
5. **Reports** — `WeighmentSlipDocument` (QuestPDF with QR code), `ExcelExporter` (ClosedXML)
6. **Notifications** — `WhatsAppService` (Selenium + WebDriverManager), `AuditLogger` (Serilog + SQLite)
7. **App** — DI host wiring in App.xaml.cs, MaterialDesignThemes setup, dark/light themes, EN/HI ResourceDictionaries, all XAML views, navigation, 7-segment font
8. **KeygenTool** — Keygen form UI, `LicenseKeyGenerator` using shared HMAC secret

### Suggested first command:
```
"Start implementing the Licensing engine — MachineIdGenerator, LicenseKeyValidator, ExpiryChecker, and LicenseStore"
```

---
---

# Phase 3: Authentication & Navigation Shell
> Last updated: 2026-05-01 09:18 IST  
> Phase: **Authentication module complete — WPF Shell, Navigation, Core Auth Services**

## 1. Files Created & Moved

### src/VinayakEnterprises.Core
| File | Purpose |
|---|---|
| `Interfaces/Data/*.cs` | **Moved** all 19 Data Repository Interfaces from `Data` to `Core` layer to satisfy Clean Architecture without circular dependencies. |
| `Services/Interfaces/IAuthService.cs` | Defines login, change password, and logout contracts. |
| `Services/Interfaces/ISessionManager.cs` | Defines session tracking and auto-lock timeout events. |
| `Services/Interfaces/INavigationService.cs` | Defines view-model-based navigation for the WPF shell. |
| `Services/Implementations/AuthService.cs` | Implements BCrypt password hashing, lockout logic, and AuditLog integrations. |
| `ViewModels/MainViewModel.cs` | Shell ViewModel that holds the currently active `CurrentViewModel`. |
| `ViewModels/Auth/LoginViewModel.cs` | MVVM logic for login, license status indicator. |
| `ViewModels/Auth/ChangePasswordViewModel.cs` | MVVM logic for forced password changes. |
| `ViewModels/DashboardViewModel.cs` | Placeholder dashboard for post-login navigation. |

### src/VinayakEnterprises.App (WPF)
| File | Purpose |
|---|---|
| `Services/NavigationService.cs` | Implements `INavigationService` by updating `MainViewModel.CurrentViewModel`. |
| `Services/WpfSessionManager.cs` | Implements `ISessionManager` using `DispatcherTimer` and `ComponentDispatcher.ThreadPreprocessMessage` to monitor keyboard/mouse activity for the 30-min auto-lock. |
| `Converters/BooleanConverters.cs` | Contains `InverseBooleanConverter` and `StringToVisibilityConverter`. |
| `Converters/RoleToVisibilityConverter.cs` | Value converter for role-based UI element visibility. |
| `Views/Auth/LoginView.xaml` / `.cs` | Material Design login user control. |
| `Views/Auth/ChangePasswordView.xaml` / `.cs` | Material Design change password user control. |
| `Views/Dashboard/DashboardView.xaml` / `.cs` | Placeholder dashboard user control. |

## 2. Features Completed
| # | Feature | Status |
|---|---|---|
| 1 | Database seeding of `Operator`, `Technician`, `Owner` roles | ✅ Done |
| 2 | Database seeding of default `admin` (`admin123`) user with `ForcePasswordChange = true` | ✅ Done |
| 3 | Core `User` model updated with `IsLocked` and `FailedLoginAttempts` | ✅ Done |
| 4 | BCrypt password hashing & verification via `AuthService` | ✅ Done |
| 5 | Account lockout after 5 consecutive failed login attempts | ✅ Done |
| 6 | 30-Minute session auto-lock with global input hooking | ✅ Done |
| 7 | WPF `MainWindow` restructured as a single-host `ContentControl` for UserControls | ✅ Done |
| 8 | Role-based visibility converter for future menu bindings | ✅ Done |
| 9 | `dotnet build` — 0 errors, 0 warnings across all 9 projects | ✅ Verified |

## 3. What Was In Progress When We Stopped
**Nothing was in progress.** The Authentication module and Navigation Shell phase completed fully.  
The solution compiles successfully with 0 errors. The application is now capable of navigating between the Login, Change Password, and Dashboard views entirely within the `MainWindow`.

## 4. Decisions Made (Not in PROMPT.md)
| Decision | What PROMPT.md Says | What Was Actually Done | Why |
|---|---|---|---|
| **Navigation Style** | Not explicitly stated | **Single-Host Window with UserControls** | The user approved a request to keep a single `MainWindow` and swap `UserControls` inside it, ensuring that global Material Design themes and contexts apply cleanly without popups. |
| **Account Lockout** | Not mentioned | **5 Failed Attempts = Account Lock** | The user explicitly requested an account lockout feature (`IsLocked`) to be toggled after 5 consecutive failed logins. |
| **Session Timeout Detection** | "Session auto-lock after 30 min" | **ComponentDispatcher.ThreadPreprocessMessage Hook** | Implemented global mouse/keyboard hooking in WPF to reliably reset the session timer without needing per-window event handlers. |
| **Clean Architecture Interfaces** | N/A | **Moved Data Interfaces to Core** | `AuthService` (in Core) requires data access. To prevent circular dependencies, all data repository interfaces were moved from `VinayakEnterprises.Data` to `VinayakEnterprises.Core/Interfaces/Data`. |

## 5. Next Step to Continue From
### Phase: **Licensing or Additional UI Views**

Recommended implementation order:
1. **Licensing** — `MachineIdGenerator` (WMI → SHA-256), `LicenseKeyValidator` (HMAC-SHA256), `LicenseStore` (DPAPI + registry), `ExpiryChecker` (startup + 24hr background).
2. **Hardware Integration** — `SerialWeightReader`, `UsbWeightReader`, `TcpWeightReader`, weight polling loop.
3. **Slip Entry Module** — The core weighbridge ticket entry screen, including large 7-segment digital weight display and background ANPR.

### Suggested first command:
```
"Start implementing the Licensing engine — MachineIdGenerator, LicenseKeyValidator, ExpiryChecker, and LicenseStore"
```
