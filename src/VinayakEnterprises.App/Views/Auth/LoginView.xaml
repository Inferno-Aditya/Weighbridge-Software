<UserControl x:Class="VinayakEnterprises.App.Views.Auth.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VinayakEnterprises.App.Views.Auth"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024">
    <Grid>
        <materialDesign:Card Width="400" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="32">
            <StackPanel>
                <TextBlock Text="VinayakEnterprises" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,8" />
                <TextBlock Text="Weighbridge System" FontSize="16" Foreground="{DynamicResource MaterialDesignToolTipBackground}" HorizontalAlignment="Center" Margin="0,0,0,32" />

                <TextBox materialDesign:HintAssist.Hint="Username" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                <PasswordBox x:Name="TxtPassword" materialDesign:HintAssist.Hint="Password" PasswordChanged="TxtPassword_PasswordChanged" Margin="0,0,0,24" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" />

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,16" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />

                <Button Content="LOGIN" Command="{Binding LoginCommand}" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" IsDefault="True" Margin="0,0,0,32" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <materialDesign:PackIcon Kind="ShieldCheck" Foreground="Green" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBlock Text="{Binding LicenseStatus}" VerticalAlignment="Center" Foreground="LightGray" />
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
